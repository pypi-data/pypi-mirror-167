def parse(folder_names):
    """
    Print a line about parsing from a folder.
    Args:
        folder_names (list): name of folder(s) where streaming data is stored
    Returns:
        None
    """

    for name in folder_names:
        print(f"I'll parse you one day, {name}.")