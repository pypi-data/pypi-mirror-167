from setuptools import setup

setup(name='distributions_prob_tiamiyu',
      version='1.0',
      description='Gaussian distributions',
      packages=['distributions_prob_tiamiyu'],
      zip_safe=False)
