from setuptools import setup

setup(name='yw_distr',
      version='0.1',
      description='Gaussian and binomial distributions',
      author = 'Youcheng Wang', 
      packages=['yw_distr'],
      zip_safe=False)
